sudo: false
language: php

cache:
  directories:
    - $HOME/.composer/cache

matrix:
  fast_finish: true
  include:
    - php: 8.0

install:
  - travis_retry composer install --no-interaction --prefer-dist

script:
  - ./vendor/bin/phpunit
